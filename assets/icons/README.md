# Icons

The site currently uses inline SVG icons defined directly in each HTML page
(no external icon files), so this folder is reserved for future use — for
example if you later want a favicon set, app icons, or a custom icon font.
